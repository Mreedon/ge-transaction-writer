package net.runelite.client.plugins.gewriter;

import net.runelite.client.config.Config;
import net.runelite.client.config.ConfigGroup;

@ConfigGroup("gewriter")
public interface GETransactionWriterConfig extends Config
{
    // Placeholder – will add toggles in Phase 2.2 (fields, mode, etc.)
}
